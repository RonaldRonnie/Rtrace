% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reporter_html.R
\name{render_layer_graph_svg}
\alias{render_layer_graph_svg}
\title{Render a layer dependency graph as an inline SVG diagram}
\usage{
render_layer_graph_svg(layers, layer_graph = list(), width = 560, height = 420)
}
\arguments{
\item{layers}{Character vector of layer names to draw as nodes. Returns
\code{NULL} if empty.}

\item{layer_graph}{A named list as in
\code{context$dependency_graph$layer_graph} (layer name -> character
vector of layer names it depends on).}

\item{width, height}{Integer pixel dimensions of the SVG viewport.}
}
\value{
Character scalar \verb{<svg>...</svg>} markup, or \code{NULL} if \code{layers}
is empty.
}
\description{
A simple, dependency-free circular layout (nodes placed evenly around a
circle): not a general graph-layout algorithm, and edge/label overlap is
not optimized away, but it's legible for the small-to-moderate layer
counts (well under 15) typical of an RTrace \verb{layers:} configuration. No
external JS/CSS — pure inline SVG, consistent with \code{\link[=reporter_html]{reporter_html()}}
being a single standalone file.
}
\details{
Edges that participate in a cycle (per \code{\link[=find_cycles]{find_cycles()}}) are drawn in
red; all other edges are drawn in gray.
}
