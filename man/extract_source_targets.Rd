% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dependency_graph.R
\name{extract_source_targets}
\alias{extract_source_targets}
\title{Extract \code{source()} target file paths, resolved to absolute paths}
\usage{
extract_source_targets(ast, base_dir, root = NULL)
}
\arguments{
\item{ast}{An \code{rtrace_file_ast}.}

\item{base_dir}{Fallback directory the relative \code{source()} argument is
resolved against (the sourcing file's own directory) when a
project-root-relative match does not exist.}

\item{root}{Optional project root, tried first (see \code{\link[=build_dependency_graph]{build_dependency_graph()}}).}
}
\value{
Character vector of normalized absolute paths (only for targets
that exist on disk).
}
\description{
Only string-literal \code{source("...")} targets are resolved; dynamically
constructed paths (e.g. \code{source(file.path(...))}) are skipped, a
documented limitation (see ADR 0002).
}
