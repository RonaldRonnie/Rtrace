% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parser.R
\name{parse_file}
\alias{parse_file}
\title{Parse an R source file}
\usage{
parse_file(path)
}
\arguments{
\item{path}{Character scalar path to an R source file.}
}
\value{
An object of class \code{rtrace_file_ast} with fields \code{path}, \code{expr}
(the parsed \code{expression} object, or \code{NULL}), \code{parse_data} (a
\code{data.frame} from \code{getParseData()}, or \code{NULL}), \code{lines} (character
vector of source lines), and \code{error} (a condition object, or \code{NULL}).
}
\description{
Wraps base R's \code{parse(keep.source = TRUE)} and \code{utils::getParseData()}.
Deliberately depends on no third-party AST representation (see ADR 0001
in \verb{dev/adr/} in the package source) so RTrace can be used alongside
\code{lintr} without internal coupling.
}
\details{
Syntax errors are captured rather than raised: the returned object's
\code{error} field is non-\code{NULL} and \code{expr}/\code{parse_data} are \code{NULL}, so a
single broken file does not abort a project-wide scan.
}
