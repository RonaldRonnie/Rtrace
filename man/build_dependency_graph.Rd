% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dependency_graph.R
\name{build_dependency_graph}
\alias{build_dependency_graph}
\title{Build a project dependency graph}
\usage{
build_dependency_graph(files, asts, root = NULL)
}
\arguments{
\item{files}{A \code{data.frame} as returned by \code{\link[=scan_files]{scan_files()}} (\code{path},
\code{rel_path}, \code{layer}).}

\item{asts}{A named list of \code{rtrace_file_ast}, keyed by \code{path}, one entry
per row of \code{files}.}

\item{root}{Character scalar, the project root \code{source()} arguments are
tried against first (the dominant convention: analysis scripts are run
from the project root and \code{source()} project-root-relative paths).
Falls back to resolving relative to the sourcing file's own directory
if no project-root-relative match exists.}
}
\value{
A list with \code{package_imports} (named list: \code{rel_path} ->
character vector of imported package names) and \code{layer_graph} (named
list: layer name -> character vector of layer names it references).
}
\description{
Produces two graphs from a set of parsed files: a package-level graph
(which CRAN/Bioconductor packages each file imports) and a layer-level
graph (which configured layers reference which other layers, derived
from \code{source()} calls whose target resolves to a file in a different
layer). See ADR 0002 in \verb{dev/adr/} in the package source for the
resolution heuristics and known limitations.
}
