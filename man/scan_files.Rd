% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/walker.R
\name{scan_files}
\alias{scan_files}
\title{Walk a project directory for R source files}
\usage{
scan_files(root, config = default_config())
}
\arguments{
\item{root}{Character scalar, path to the project root.}

\item{config}{An \code{rtrace_config} object (see \code{\link[=read_config]{read_config()}}).}
}
\value{
A \code{data.frame} with columns \code{path} (absolute), \code{rel_path}
(POSIX-style, relative to \code{root}), and \code{layer} (character;
\code{"(unassigned)"} if no configured layer matches).
}
\description{
Discovers \code{.R}/\code{.r} files under \code{root}, skipping \code{\link[=default_excludes]{default_excludes()}}
plus any patterns declared in \code{config$exclude}, then assigns each
surviving file to a configured layer via longest-glob-prefix match
against \code{config$layers}.
}
