% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{build_api_router}
\alias{build_api_router}
\title{Build the plumber router without starting it}
\usage{
build_api_router(
  token = Sys.getenv("RTRACE_API_TOKEN", ""),
  allowed_roots = getwd()
)
}
\arguments{
\item{token}{Character scalar. Bearer token required on every request via
\verb{Authorization: Bearer <token>}. Default: the \code{RTRACE_API_TOKEN}
environment variable. If empty (the default when the variable is
unset), no authentication is enforced -- callers embedding the router
directly are responsible for exposure control in that case.}

\item{allowed_roots}{Character vector of directories that \code{root} request
parameters are allowed to resolve within (self or descendant). Default:
the current working directory.}
}
\value{
A \code{plumber::Plumber} router object.
}
\description{
Useful for testing, embedding in a larger plumber app, or customizing
routes before calling \verb{$run()}.
}
