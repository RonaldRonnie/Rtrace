% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rule_engine.R
\name{run_rules}
\alias{run_rules}
\title{Run the configured rule set against a context}
\usage{
run_rules(context)
}
\arguments{
\item{context}{An \code{rtrace_context} (see \code{\link[=build_context]{build_context()}}).}
}
\value{
An \code{rtrace_diagnostic_set}.
}
\description{
Resolves the active rule set from \code{context$config$rules}, evaluates each
enabled rule's \link{Rule}\verb{$check()}, tags every diagnostic with the rule's
\emph{configured} severity (falling back to the rule's default severity), and
collects results into a single \code{rtrace_diagnostic_set}.
}
\details{
A rule that errors during evaluation does not abort the scan: its error
is captured and surfaced as a single \code{rule-error} diagnostic so the rest
of the rule set still runs.
}
