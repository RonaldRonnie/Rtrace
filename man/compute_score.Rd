% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scoring.R
\name{compute_score}
\alias{compute_score}
\title{Compute a 0-100 quality score from a diagnostic set}
\usage{
compute_score(
  diagnostics,
  error_penalty = 10,
  warning_penalty = 3,
  info_penalty = 1,
  baseline = 100
)
}
\arguments{
\item{diagnostics}{An \code{rtrace_diagnostic_set}.}

\item{error_penalty}{Numeric, score deducted per error.}

\item{warning_penalty}{Numeric, score deducted per warning.}

\item{info_penalty}{Numeric, score deducted per info.}

\item{baseline}{Numeric, starting score (default 100).}
}
\value{
A \code{trace_score} object.
}
\description{
The scoring model:
\itemize{
\item Start at 100 (perfect).
\item Subtract \code{error_penalty} per \code{error}-severity diagnostic (default 10).
\item Subtract \code{warning_penalty} per \code{warning}-severity diagnostic (default 3).
\item Subtract \code{info_penalty} per \code{info}-severity diagnostic (default 1).
\item Clamp to [0, 100].
}
}
\details{
The penalties are intentionally modest defaults so that one error does not
collapse a score to zero on a large project. Pass custom penalties to tune
the scoring model for a specific module's domain.
}
