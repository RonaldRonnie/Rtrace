% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parser.R
\name{find_qualified_calls}
\alias{find_qualified_calls}
\title{Find all namespace-qualified calls to a given \code{pkg::fn} (or \code{pkg:::fn})}
\usage{
find_qualified_calls(ast, pkg, fn_name)
}
\arguments{
\item{ast}{An \code{rtrace_file_ast}.}

\item{pkg}{Character scalar package name (e.g. \code{"reshape2"}).}

\item{fn_name}{Character scalar function name (e.g. \code{"melt"}).}
}
\value{
A \code{data.frame} with columns \code{line1}, \code{col1} (position of the
\code{pkg} token), one row per call site. Zero rows if the file failed to
parse or contains no such calls.
}
\description{
Find all namespace-qualified calls to a given \code{pkg::fn} (or \code{pkg:::fn})
}
