% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache.R
\name{ast-cache}
\alias{ast-cache}
\title{Incremental scanning: AST parse cache}
\description{
Caches parsed \code{rtrace_file_ast} objects (see \code{\link[=parse_file]{parse_file()}}) on disk, keyed by an MD5
content hash per file, so repeated scans of a project (interactive
iteration, or CI runs across nearby commits) skip re-parsing files whose
content hasn't changed. This caches the \emph{parse} step only — diagnostics
are always recomputed for the full project on every scan, because most
built-in rules read cross-file state (the dependency graph, all test
files for \code{testing.missingTests}, etc.) and a stale per-file diagnostic
cache could silently miss a violation introduced by a \emph{different} file
changing. See \href{https://github.com/RonaldRonnie/Rtrace/blob/main/dev/roadmap.md}{dev/roadmap.md}
for the rule-scope model a future per-rule diagnostic cache would need.
}
\details{
Caching is opt-in (\code{use_cache = TRUE} to \code{\link[=build_context]{build_context()}}/\code{\link[=run_scan]{run_scan()}},
or \code{--cache} on the CLI), not a silent default, so calling RTrace from
an R script or test never writes files to disk unless asked.
}
