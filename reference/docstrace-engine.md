# DocsTrace Engine

Evaluates documentation quality for an R project or package. Goes beyond
the existing `documentation.missing` rule (which only checks whether a
roxygen2 block exists above each function) to assess README
completeness, vignette coverage, pkgdown configuration, and the quality
of examples.
