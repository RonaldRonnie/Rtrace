# Convert a diagnostic set to a data frame

Convert a diagnostic set to a data frame

## Usage

``` r
# S3 method for class 'rtrace_diagnostic_set'
as.data.frame(x, ...)
```

## Arguments

- x:

  An `rtrace_diagnostic_set`.

- ...:

  Unused.

## Value

A `data.frame` with one row per diagnostic.
