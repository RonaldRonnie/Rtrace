# Set the active recommendation provider

Set the active recommendation provider

## Usage

``` r
set_recommendation_provider(id)
```

## Arguments

- id:

  Character scalar provider id. Use `"builtin"` for the deterministic
  built-in provider (the default).

## Value

Invisibly, the previous active provider id.
