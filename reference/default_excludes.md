# Default directories/files RTrace never scans

Default directories/files RTrace never scans

## Usage

``` r
default_excludes()
```

## Value

Character vector of glob patterns, relative to the project root.
