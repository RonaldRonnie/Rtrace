# Convert an absolute path to a project-relative path, for diagnostics

Convert an absolute path to a project-relative path, for diagnostics

## Usage

``` r
relative_path(context, path)
```

## Arguments

- context:

  An `rtrace_context`.

- path:

  Character scalar absolute path.

## Value

Character scalar relative path.
