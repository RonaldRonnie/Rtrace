# Construct a set of diagnostics

Construct a set of diagnostics

## Usage

``` r
new_diagnostic_set(diagnostics = list())
```

## Arguments

- diagnostics:

  A list of `rtrace_diagnostic` objects.

## Value

An object of class `rtrace_diagnostic_set`.
