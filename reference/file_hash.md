# Compute a file's content hash

Compute a file's content hash

## Usage

``` r
file_hash(path)
```

## Arguments

- path:

  Character scalar file path.

## Value

Character scalar MD5 hash, or `NA_character_` if the file doesn't exist.
