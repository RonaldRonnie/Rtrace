# Get a registered module by id

Get a registered module by id

## Usage

``` r
get_module(id)
```

## Arguments

- id:

  Character scalar module id.

## Value

A module list, or `NULL` if not registered.
